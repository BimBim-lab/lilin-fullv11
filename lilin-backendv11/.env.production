# Backend Environment Variables untuk Railway
# Copy variables ini ke Railway Dashboard

# Database (akan diisi otomatis oleh Railway/Neon)
DATABASE_URL=postgresql://username:password@host:port/database

# JWT Secret (wajib diubah untuk production)
JWT_SECRET=your-super-secret-jwt-key-production-change-this

# Email Configuration (untuk production SMTP real)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Admin Credentials (opsional, bisa hardcode di code)
ADMIN_EMAIL=admin@weiscandle.com
ADMIN_PASSWORD_HASH=$2b$10$hashedpassword

# Node Environment
NODE_ENV=production

# Port (Railway akan set otomatis)
PORT=5000
